import { combineReducers } from 'redux';
import * as types from 'types';

const questions = (
    state = '',
    action
) => {
    switch (action.type) {

        default:
            return state;
    }
}
